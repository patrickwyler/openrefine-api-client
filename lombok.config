# This is the root directory and lombok shouldn’t search parent directories for
# any other configuration files.
config.stopBubbling = true

# Add the annotation @lombok.Generated to all generated methods so they are
# excluded from code coverage
lombok.addLombokGeneratedAnnotation = true
